Al-Fares-Hacker/        # repo root
├── docker-compose.yml  # تشغيل شامل (prod + dev)
├── .env.template       # يُنسخ إلى .env ويُعدَّل
├── nginx/tor.conf      # proxy + onion v3
│
├── frontend/           # React 18 + Vite + Three + Tailwind + Framer-motion
├── backend/            # NestJS 10 + Socket.IO + GraphQL
├── ai-core/            # FastAPI + Torch + ONNX + Hugging GGUF (Arabic/Mistral)
├── c2-panel/           # WebSocket gateway (meterpreter over WebSocket)
├── payload-factory/    # APK / IPA / PE / ELF builders + polymorphic engine
├── osint-engine/       # Phone, e-mail, breach, social-graph, face-search
├── geo-tracker/        # HLR-SS7 adapter + OpenCage + MLS + own Stingray-map
├── tools-bin/          # static bins (nmap, bettercap, hostapd, karma, etc.)
└── deployments/
    ├── build.sh        # one-liner build & ship
    ├── onion.sh        # generate & print onion
    └── harden.sh       # kernel hardening + iptables + fail2ban
